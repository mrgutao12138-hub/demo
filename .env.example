# 复制为 .env 后按需修改：cp .env.example .env
# 密码至少 8 位。数据只写本机，不会上传任何云。

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET=files
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001

# 本机数据目录（相对 docker-compose.yml 所在目录）
MINIO_DATA_DIR=./data
