# MinIO 本机对象存储操作手册

这套服务跑在你自己的电脑上，文件只写入本机目录 `./data`，不会上传到任何云厂商。

端口默认只监听 `127.0.0.1`，同一台机器能访问，局域网和公网访问不到。

---

## 1. 需要准备什么

- Docker Desktop（Windows / macOS）或 Docker Engine + Compose 插件（Linux）
- 本机空闲端口：`9000`（S3 API）、`9001`（网页控制台）

检查：

```bash
docker --version
docker compose version
```

---

## 2. 首次启动

在本仓库目录执行：

```bash
cp .env.example .env
docker compose up -d
```

第一次会拉镜像，稍等一会儿。看状态：

```bash
docker compose ps
docker compose logs -f minio
```

`minio` 为 `healthy`，并且 `minio-init` 退出码为 0，即启动成功。默认桶 `files` 会自动建好。

浏览器打开：http://127.0.0.1:9001

默认账号 / 密码（可在 `.env` 改）：

- 用户：`minioadmin`
- 密码：`minioadmin`

---

## 3. 文件实际写在哪

| 你看到的 | 实际位置 |
| --- | --- |
| 桶 `files` | 本机 `./data/files/` |
| 对象 `photos/a.jpg` | 本机 `./data/files/photos/a.jpg` |

改存储位置：编辑 `.env` 里的 `MINIO_DATA_DIR`，例如：

```env
MINIO_DATA_DIR=/home/你的用户名/minio-data
```

然后：

```bash
docker compose up -d
```

不要把 `./data` 提交进 Git，仓库已忽略该目录。

---

## 4. 网页控制台怎么用

打开 http://127.0.0.1:9001 并登录。

### 建桶

1. 点 **Create Bucket** / **创建存储桶**
2. 名称只能小写字母、数字、短横线，例如 `docs`、`backup`
3. 创建后即可往里传文件

启动时已自动创建 `files`，一般不用再建。

### 上传

1. 点开某个桶
2. 点 **Upload**，选文件；或直接把文件拖进去
3. 可以按「文件夹/前缀」组织，例如 `images/2026/`

### 下载 / 预览

在对象列表里点文件即可预览或下载。

### 删除

选中对象或桶，选 Delete。删除桶前需要先清空里面的对象。

---

## 5. 命令行上传下载

把 Access Key / Secret Key 设成 `.env` 里的账号密码。

### 方式 A：aws cli

```bash
export AWS_ACCESS_KEY_ID=minioadmin
export AWS_SECRET_ACCESS_KEY=minioadmin
export AWS_DEFAULT_REGION=us-east-1

# 上传
aws --endpoint-url http://127.0.0.1:9000 s3 cp ./README.md s3://files/README.md

# 列出
aws --endpoint-url http://127.0.0.1:9000 s3 ls s3://files/

# 下载
aws --endpoint-url http://127.0.0.1:9000 s3 cp s3://files/README.md ./README.downloaded.md

# 删除
aws --endpoint-url http://127.0.0.1:9000 s3 rm s3://files/README.md
```

### 方式 B：MinIO 客户端 `mc`（用现成容器，不用本机安装）

```bash
docker compose run --rm --no-deps --entrypoint /bin/sh minio-init -c '
  mc alias set local http://minio:9000 "$MINIO_ROOT_USER" "$MINIO_ROOT_PASSWORD"
  mc ls local/files
'
```

本机若已安装 `mc`：

```bash
mc alias set local http://127.0.0.1:9000 minioadmin minioadmin
mc cp ./README.md local/files/README.md
mc ls local/files
mc cat local/files/README.md
```

---

## 6. 程序里怎么接（S3 SDK）

所有官方 S3 SDK 都能用，关键配置：

- endpoint：`http://127.0.0.1:9000`
- access key / secret key：`.env` 里的用户和密码
- 强制 path-style（不要用虚拟主机风格）
- region 填任意值即可，例如 `us-east-1`

Python 示例（`boto3`）：

```python
import boto3

s3 = boto3.client(
    "s3",
    endpoint_url="http://127.0.0.1:9000",
    aws_access_key_id="minioadmin",
    aws_secret_access_key="minioadmin",
    region_name="us-east-1",
)

s3.upload_file("local.txt", "files", "local.txt")
s3.download_file("files", "local.txt", "downloaded.txt")
```

---

## 7. 改账号、端口、桶名

编辑 `.env`，例如：

```env
MINIO_ROOT_USER=myuser
MINIO_ROOT_PASSWORD=change-me-please
MINIO_BUCKET=files
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_DATA_DIR=./data
```

改完后重启：

```bash
docker compose up -d
```

密码至少 8 位。改过账号密码后，控制台和 SDK 都要用新值。

---

## 8. 日常启停

```bash
# 启动
docker compose up -d

# 看日志
docker compose logs -f minio

# 停止（数据仍在 ./data，不会删）
docker compose stop

# 停止并删容器（仍然不删 ./data）
docker compose down
```

`docker compose down -v` 在这套配置里也不会删本机 `./data`。真要清空文件，请自己删目录：

```bash
rm -rf ./data
```

---

## 9. 备份与恢复

备份就是复制本机数据目录：

```bash
tar -czf minio-backup-$(date +%F).tar.gz -C . data
```

恢复：先停服务，再解压回原位置，再启动。

```bash
docker compose stop
tar -xzf minio-backup-2026-09-10.tar.gz
docker compose up -d
```

---

## 10. 常见问题

**端口被占用**

改 `.env` 的 `MINIO_API_PORT` / `MINIO_CONSOLE_PORT`，再 `docker compose up -d`。

**控制台打不开**

确认绑定的是本机地址：用 `http://127.0.0.1:9001`，不要用局域网 IP。需要给同网段别人用时，把 `docker-compose.yml` 里的 `127.0.0.1:` 去掉，改成 `${MINIO_API_PORT:-9000}:9000` 这种形式，并自行做好防火墙。默认不建议这样做。

**上传失败 / Access Denied**

核对 Access Key、Secret Key 是否和 `.env` 一致；SDK 是否开了 path-style；endpoint 是否为 `http://127.0.0.1:9000`。

**想换本机目录**

改 `MINIO_DATA_DIR` 后重启。旧文件仍在原来的目录里，需要的话手动拷过去。

**完全卸载**

```bash
docker compose down
rm -rf ./data
```

镜像还在本机 Docker 里，不需要可再执行 `docker image rm quay.io/minio/minio:RELEASE.2025-07-23T15-54-02Z quay.io/minio/mc:RELEASE.2025-07-21T05-28-08Z`。
